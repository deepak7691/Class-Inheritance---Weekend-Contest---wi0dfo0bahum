class Shape {
  constructor(color) {
    this.color = red;
  }

  drawShape() {
    // code to draw the shape
  }

  calculateArea() {
    // code to calculate the area of the shape
  }
}

module.exports = Shape;
